import { Typography } from "@mui/material"
import React from "react"

export default function Error(){
    return <div className='container-contain'>
        <Typography variant="h5" color="primary">Select a community to join Chat</Typography>
    </div>
}